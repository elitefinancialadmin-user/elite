# Elite Financial Group — Sistema de Gestión

Aplicación de una sola página (HTML/CSS/JS) conectada a Supabase, desplegada en Vercel.

- **Frontend**: `index.html` (sin build, sin frameworks)
- **Base de datos**: Supabase (Postgres)
- **Hosting**: Vercel (auto-deploy al hacer push a `main`)
